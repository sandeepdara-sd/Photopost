/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        // Use "domains" array to specify allowed image domains
        domains: ['via.placeholder.com'],
      },
};

export default nextConfig;
